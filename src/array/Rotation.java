package array;

public class Rotation {

    static void rotateToRight(int i, int[] array) {
        if(array == null || array.length < 2) {
            return;
        }

        int numOfSub = i < array.length ? array.length / i : i / array.length;



    }
}
